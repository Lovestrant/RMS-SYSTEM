import 'package:flutter/cupertino.dart';

class ownerdashboard extends StatelessWidget {
  const ownerdashboard({Key? key, String title="Owner Dashboard"}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
